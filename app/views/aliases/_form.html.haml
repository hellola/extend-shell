= form_for @alias, html: { class: "form-horizontal", role: "form" } do |f|
  -if @alias.errors.any?
    .alert.alert-danger.alert-dismissable{role: "alert"}
      %button.close{type: "button", data: {dismiss: "alert"} }
        %span{aria: {hidden: "true"} } &times;
        %span.sr-only Close
      %h4= "#{pluralize(@alias.errors.count, "error")} prohibited this alias from being saved:"

      %ul
        - @alias.errors.full_messages.each do |msg|
          %li= msg

  .form-group
    = f.label :name, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :name, class: 'form-control'
  .form-group
    = f.label :command, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :command, class: 'form-control'
  .form-group
    = f.label :location, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :location, class: 'form-control'
  .form-group
    = f.label :operating_system, class: 'col-sm-2 control-label'
    .col-sm-10
      = f.text_field :operating_system, class: 'form-control'
  .form-group
    .col-sm-offset-2.col-sm-10
      = f.submit class: 'btn btn-primary'
